{% if fondy_data.message %}
<div class="alert alert-warning">
  {{ fondy_data.message }}
  <button type="button" class="close" data-dismiss="alert">×</button>
</div>
{% else %}
<form action="{{ url }}" method="post">
        <input type="hidden" name="merchant_id" value="{{ fondy_data.merchant_id }}">
        <input type="hidden" name="order_id" value="{{ fondy_data.order_id }}">
        <input type="hidden" name="order_desc" value="{{ fondy_data.order_desc }}">
        <input type="hidden" name="amount" value="{{ fondy_data.amount }}">
        <input type="hidden" name="currency" value="{{ fondy_data.currency }}">
        <input type="hidden" name="response_url" value="{{ fondy_data.response_url }}">
        <input type="hidden" name="server_callback_url" value="{{ fondy_data.server_callback_url }}">
        <input type="hidden" name="sender_email" value="{{ fondy_data.sender_email }}">
        <input type="hidden" name="lang" value="{{ fondy_data.lang }}">
        <input type="hidden" name="signature" value="{{ fondy_data.signature }}">
        <div class="buttons">
            <div class="pull-right">
                <input type="submit" value="{{ button_confirm }}" class="btn btn-primary"/>
            </div>
        </div>
    </form>
{% endif %}