{% if fondy_data.message %}
<div class="alert alert-warning">
  {{ fondy_data.message }}
  <button type="button" class="close" data-dismiss="alert">×</button>
</div>
{% else %}
<div class="buttons">
  <div class="pull-right">
    <a href="{{ fondy_data.url }}" class="btn btn-primary">{{ button_confirm }}</a>
  </div>
</div>
{% endif %}